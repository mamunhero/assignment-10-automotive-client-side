import { useLoaderData } from "react-router-dom";

const Samsung = () => {
  const brands = useLoaderData();
  console.log(brands);
  return (
    <div>
      
    </div>
  );
};

export default Samsung;